# raylib game template

## building

without just (and with bash):

```
sh build.sh <Debug|Release>
```

with just:

```
just build-<debug|release>
```

## playing

without just:

```
./build/game
```

with just:

```
just play
```
